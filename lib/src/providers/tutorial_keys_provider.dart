import 'package:flutter/material.dart';

class TutorialKeysProvider with ChangeNotifier {
  final GlobalKey keyHomePage = GlobalKey();
  final GlobalKey keyPatrimony = GlobalKey();
  final GlobalKey keyUpload = GlobalKey();
}